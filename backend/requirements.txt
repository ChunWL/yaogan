# =============================================================================
# FastAPI Web框架
# =============================================================================
# Web应用核心框架，提供异步API路由、请求验证等
fastapi>=0.104.0

# ASGI服务器，用于运行FastAPI应用
uvicorn[standard]>=0.24.0

# 文件上传支持，FastAPI处理multipart/form-data必需
python-multipart>=0.0.6

# 数据验证和配置管理
pydantic>=2.5.0
pydantic-settings>=2.1.0

# =============================================================================
# 数据库
# =============================================================================
# SQLAlchemy ORM框架
sqlalchemy>=2.0.0

# PostgreSQL数据库驱动
psycopg2-binary>=2.9.0

# =============================================================================
# Redis缓存
# =============================================================================
# Redis Python客户端
redis>=5.0.0

# =============================================================================
# MinIO对象存储
# =============================================================================
# AWS S3 SDK（兼容 MinIO / Supabase Storage / Cloudflare R2）
boto3>=1.35.0
botocore>=1.35.0

# =============================================================================
# YOLO目标检测与图像处理
# =============================================================================
# PyTorch CPU版（Render免费计划无GPU，CPU版节省约2GB内存）
# 通过 --index-url https://download.pytorch.org/whl/cpu 安装
torch>=2.0.0
torchvision>=0.15.0
# Ultralytics YOLO框架
ultralytics>=8.0.0

# OpenCV图像处理库
opencv-python>=4.8.0

# Pillow图像处理库
pillow>=10.0.0

# =============================================================================
# 工具库
# =============================================================================
# 环境变量加载
python-dotenv>=1.0.0

# NumPy数值计算（ultralytics自动安装，此处指定版本范围）
numpy>=1.23.0,<2.0.0

# =============================================================================
# JWT认证
# =============================================================================
python-jose[cryptography]>=3.3.0

# 密码哈希
passlib[bcrypt]>=1.7.4